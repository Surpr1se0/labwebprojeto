@model IEnumerable<labwebprojeto.Models.Jogo>

@{
    ViewData["Title"] = "Index";
}



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/utadlabwebprojeto.styles.css" asp-append-version="true" />
    <link href="~/css/site.css" rel="stylesheet" />
</head>

<body id="bloquear-selecao" style="background-color:#410698">


    <section id="Second">

        <div class="containerIndex">
            <div class="row">

                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div class="card mb-3 box-shadow">
                            <a asp-controller="Jogos" asp-action="Details" asp-route-id="@item.IdJogos">
                                <img class="card-img-top" src="@item.Foto" alt="Imagem Exemplo">
                            </a>
                            <div class="colocarladoeasyway">
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-10">
                                            <p>@item.Nome.ToUpper()</p>
                                        </div>
                                        <div class="col-2">
                                            <p>@Math.Round(@item.Preco,2)€</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="d-flex">
                            <div class="btn-group">
                                @if ((User.IsInRole("Admin")) || (User.IsInRole("Client")) || (User.IsInRole("Func")))
                                {
                                    @*<div class="btn-group">
                            <a type="btn btn-success" button asp-controller="Home" asp-action="Details" asp-route-id="@item.IdJogos" class="btn btn-sm btn-outline-secondary">
                            Buy
                            </a>
                            </div>*@

                                }
                                else
                                {
                                    @*<a type="btn btn-success" button asp-controller="Home" asp-action="Details" asp-route-id="@item.IdJogos" class="btn btn-sm btn-outline-secondary">
                            Details
                            </a>*@
                                }

                            </div>
                        </div>

                    </div>

                }
            </div>

        </div>


    </section>

    <script src="https://kit.fontawesome.com/d7325001d6.js" crossorigin="anonymous"></script>























    @*<section class="jumbotron text-center">
    <div class="container">
    <h1 class="jumbotron-heading">Can't Stop</h1>
    <p class="lead">A website <strong> for gamers</strong> by gamers.</p>
    <p>
    </p>
    </div>

    @*        <div>
    <h1>@ViewData["IdUtilizador"]</h1>
    </div>

    </section>

    <div class="containerIndex">
    <div class="row">

    @foreach (var item in Model)
    {
    <div class="col-md-4">
    <div class="card mb-3 box-shadow">
    <img class="card-img-top" src="@item.Foto" width="100%" height="235" alt="Imagem Exemplo">
    <div class="card-body">
    <p class="card-text">@item.Nome.ToUpper()</p>
    @*<h5 class="card-text2">@item.Descricao</h5>*@
    @*<input value="@Math.Round(@item.Preco,2)€">

    </div>

    </div>
    <div class="d-flex">
    <div class="btn-group">
    @if ((User.IsInRole("Admin")) || (User.IsInRole("Client")) || (User.IsInRole("Func")))
    {
    <div class="btn-group">
    <a type="btn btn-success" button asp-controller="Home" asp-action="Details" asp-route-id="@item.IdJogos" class="btn btn-sm btn-outline-secondary">
    Buy
    </a>
    </div>

    }
    else {
    <a type="btn btn-success" button asp-controller="Home" asp-action="Details" asp-route-id="@item.IdJogos" class="btn btn-sm btn-outline-secondary">
    Details
    </a>
    }

    </div>
    </div>

    </div>
    }
    </div>
    </div>*@
</body>
</html>